import { Mail } from 'lucide-react';
import { Button } from '../../components/ui/button';
import Image from 'next/image';

export default function Home() {
    return (
        <div>
            <Image src="/images/fake.png" alt="hero" width={1920} height={1080} />
        </div>
    );
}
